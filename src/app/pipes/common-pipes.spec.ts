import { ObjectKeysPipe } from './common-pipes';

describe('ObjectKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
